# Verification evidence (not claims)

Worktree: `/Users/jalon/runtime-analyze`  
Generated by actually installing and running the tools below. Failures are reported as failures.

## Tool install (what ran)

| Tool | Source | Installed? |
|------|--------|------------|
| ruff | astral-sh/ruff | yes (dev dep / venv) |
| mypy | python/mypy | yes (dev dep / venv) |
| hypothesis | HypothesisWorks/hypothesis | yes (`uv pip install hypothesis`) |
| atheris | google/atheris | **not installed** (needs libFuzzer toolchain); replaced by `tests/test_importers_fuzz.py` garbage corpus + isolation test |
| mutmut | boxed/mutmut | yes (`uv pip install mutmut`) |
| memray | bloomberg/memray | yes (`uv pip install memray`) |
| pytest-benchmark | ionelmc/pytest-benchmark | installed; scale numbers recorded in `perf.json` instead of benchmark plugin markers |

## Lint / types

| Check | Result | Log |
|-------|--------|-----|
| ruff (importers + tests) | **PASS** | `evidence/ruff.log` |
| mypy `gitm/importers` | **PASS** | `evidence/mypy.log` |

## Pytest

| Suite | Result | Log |
|-------|--------|-----|
| full `tests/` | **PASS** (exit 0) | `evidence/full_suite.log` |
| importers + hypothesis + fuzz + rollup math | **PASS** | `evidence/tests_after_math.log` / `evidence/hypothesis_fuzz.log` |

### Hypothesis found real bugs (fixed)

`test_prop_detect_garbage_bytes` falsified:

1. Truncated `SQLite format 3\x00` → `sqlite3.DatabaseError` escaped `detect_format`
2. Truncated gzip `\x1f\x8b` → `EOFError` escaped `detect_format`

Both are fixed in `gitm/importers/detect.py` (catch `sqlite3.Error` on table probe; catch `EOFError`/`BadGzipFile` on gzip).

## Mutmut (the one that mattered)

Scoped to `gitm/importers/node_rollup.py` with `tests/test_node_rollup_math.py` + importer tests.

**Result (mutmut 3 progress line):**

```
226/226  🎉 176 killed  🙁 50 survived
```

| Metric | Value |
|--------|-------|
| Mutants | 226 |
| Killed | **176** (~78%) |
| Survived | **50** (~22%) |
| Log | `evidence/mutmut_node_rollup.log` |
| Survivor list | `evidence/mutmut_results.txt` |

Earlier run on `detect.py` + `node_rollup.py` **without** the math tests produced a huge survivor list (hollow green). Adding `tests/test_node_rollup_math.py` moved kill rate from ~0% to ~78% on the rollup module. **50 survivors remain** — the suite is better but not tight enough to claim full field-mapping coverage.

First mutmut attempt on `detect.py` alone also listed many survivors (error-path string/branch mutants often don't flip observable test outcomes).

## Memray

| Input | Result |
|-------|--------|
| 200k-kernel chrome-trace analyze | profile written `evidence/memray_200k.bin` |
| Summary | `evidence/memray_summary.txt` — on the order of **~1 GiB** total tracked allocations; top frames are `import_torch_trace` / `finish_trace` / `normalize_and_clean` / pydantic `model_copy` |

**Conclusion from memray:** events are fully materialized in RAM. This is **not** a pure streaming pipeline end-to-end.

## Perf target (§7: 5M kernels, &lt;5 min, &lt;4 GiB)

Measured in **fresh processes** (`evidence/perf.json`):

| Kernels | wall_s | peak_rss_GiB | kernels imported |
|--------:|-------:|-------------:|-----------------:|
| 50_000 | 1.47 | 0.195 | 50_000 |
| 200_000 | 6.68 | 0.584 | 200_000 |
| 500_000 | 21.69 | 1.219 | 500_000 |

Linear extrapolation to **5_000_000**:

| | Estimate | Pass target? |
|--|----------|--------------|
| wall | ~217 s (~3.6 min) | yes (&lt;300 s) |
| RSS | **~12.2 GiB** | **NO** (&lt;4 GiB) |

**`passed_5m_targets: false`.** A full 5M run was **not** executed on this host because projected RSS would OOM; that is stated in `perf.json`, not hand-waved.

Also found: the brace-streaming parser under-counted events on compact multi-line dumps; load threshold was raised so correctness wins up to 400 MiB. Streaming remains a fallback for multi-GB inputs only.

## Atheris

Not run as libFuzzer. Equivalent coverage exercised via:

- `tests/test_importers_fuzz.py` — fixed garbage corpus against `detect_format` / import / multi-file isolation
- hypothesis binary garbage against `detect_format`

## Customer report artifact

`evidence/real_fixture_report.md` — rendered from real kineto fixtures + synthetic 4×A100 NCCL trace.

## Bottom line (honest)

| Bar | Status |
|-----|--------|
| ruff + mypy clean | **yes** |
| pytest green | **yes** |
| hypothesis useful (found bugs) | **yes** |
| mutmut kill rate acceptable | **partial** — 78% on node_rollup; 50 survivors left |
| 5M / 4 GiB | **failed projection** |
| “tests green ⇒ correct” | **disproven by mutmut until math tests; still not fully disproven for survivors** |
